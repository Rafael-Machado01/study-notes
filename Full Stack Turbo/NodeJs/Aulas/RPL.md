node ai entra no terminal igual do navegegador

API NodeJS: [https://nodejs.org/api/](https://nodejs.org/api/)

Importando uma api 
const fs = require ('fs')
FileSystem

```
const fs = require ('fs')
fs.copyFileSync('helloworld.txt', 'hi.txt') // Copiando e criando arquivos
````

